{
  "type": "added",
  "message": "Standard optional `repositorySource` and `repositoryRevision` configuration keys for schema-advertised, host-owned repository-backed session creation through existing configuration and lifecycle messages."
}
