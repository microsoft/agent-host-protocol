{
  "type": "changed",
  "message": "`ChatState.title` and `ChatSummary.title` are now optional. An absent title means the chat has no title of its own and consumers should fall back to the owning session's title — the normal case for a session's default chat. Producers must omit the field rather than sending an empty string to mean \"inherit\"."
}
